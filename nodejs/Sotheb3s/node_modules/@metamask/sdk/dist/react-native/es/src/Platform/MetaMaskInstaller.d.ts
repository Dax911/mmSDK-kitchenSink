import { ProviderService } from '../services/ProviderService';
import { PlatformManager } from './PlatfformManager';
interface InstallerProps {
    preferDesktop: boolean;
    remote: ProviderService;
    platformManager: PlatformManager;
    debug?: boolean;
}
/**
 * Singleton class instance
 */
export declare class MetaMaskInstaller {
    private static instance;
    private isInstalling;
    private hasInstalled;
    private resendRequest;
    private preferDesktop;
    private platformManager;
    private remote;
    private debug;
    constructor({ preferDesktop, remote, platformManager, debug, }: InstallerProps);
    startDesktopOnboarding(): void;
    redirectToProperInstall(): Promise<boolean>;
    checkInstallation(): Promise<boolean>;
    start({ wait }: {
        wait: boolean;
    }): Promise<boolean>;
}
export {};
//# sourceMappingURL=MetaMaskInstaller.d.ts.map